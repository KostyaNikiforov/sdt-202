import deque.DequeClient;
import queue.QueueClient;

// Name, Version, completion time, description, ai?
public class Main {
    public static void main(String[] args) {
        QueueClient.main(args);
        DequeClient.main(args);
    }
}
